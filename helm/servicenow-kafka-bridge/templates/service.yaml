apiVersion: v1
kind: Service
metadata:
  name: {{ include "servicenow-kafka-bridge.fullname" . }}
  labels:
    {{- include "servicenow-kafka-bridge.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "servicenow-kafka-bridge.selectorLabels" . | nindent 4 }}
